<Application x:Class="WPF_Main.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPF_Main"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Paleta -->
        <SolidColorBrush x:Key="MDNBackground" Color="#393473" />
        <SolidColorBrush x:Key="MDNPanel" Color="#2A2559" />
        <SolidColorBrush x:Key="MDNBorder" Color="#51498C" />
        <SolidColorBrush x:Key="MDNText" Color="#FFFFFF" />
        <SolidColorBrush x:Key="MDNAccent" Color="#FF7139" />
        <SolidColorBrush x:Key="MDNAccentHover" Color="#FF905E" />
        <SolidColorBrush x:Key="MDNPrimaryButton" Color="#312a64" />
        <SolidColorBrush x:Key="MDNPrimaryButtonHover" Color="#1d1844" />
        <SolidColorBrush x:Key="MDNPrimaryButtonPressed" Color="#110d2b" />

        <!-- TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MDNText}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource MDNPanel}" />
            <Setter Property="Foreground" Value="{StaticResource MDNText}" />
            <Setter Property="BorderBrush" Value="{StaticResource MDNBorder}" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="{StaticResource MDNText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource MDNAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button-->
        <Style x:Key="MDN" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource MDNPrimaryButton}"/>
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource MDNPrimaryButtonHover}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource MDNPrimaryButtonPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button -->
        <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource MDN}">
            <Setter Property="Background" Value="{StaticResource MDNAccent}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource MDNAccentHover}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource MDNPanel}" />
            <Setter Property="RowBackground" Value="{StaticResource MDNPanel}" />
            <Setter Property="AlternatingRowBackground" Value="{StaticResource MDNPrimaryButton}" />
            <Setter Property="Foreground" Value="{StaticResource MDNText}" />
            <Setter Property="BorderBrush" Value="{StaticResource MDNBorder}" />
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource MDNBorder}" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource MDNPrimaryButtonHover}" />
            <Setter Property="Foreground" Value="{StaticResource MDNText}" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="{StaticResource MDNBorder}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource MDNText}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNAccent}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="Transparent" 
                                BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox & RadioButton -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ComboBox & DatePicker -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="Background" Value="{StaticResource MDNPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource MDNBorder}"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Style.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{StaticResource MDNText}" />
                </Style>
            </Style.Resources>
            <Setter Property="Background" Value="{StaticResource MDNPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNAccent}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="Background" Value="{StaticResource MDNPanel}"/>
        </Style>

        <!-- Calendar redefinit -->
        <Style TargetType="DatePicker">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="Background" Value="{StaticResource MDNPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource MDNBorder}"/>
            <Setter Property="CalendarStyle">
                <Setter.Value>
                    <Style TargetType="Calendar">
                        <Setter Property="Background" Value="{StaticResource MDNPanel}"/>
                        <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
                        <Setter Property="CalendarItemStyle">
                            <Setter.Value>
                                <Style TargetType="CalendarItem">
                                    <Setter Property="Margin" Value="0,3,0,3" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="CalendarItem">
                                                <ControlTemplate.Resources>
                                                    <DataTemplate x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type CalendarItem}, ResourceId=DayTitleTemplate}">
                                                        <TextBlock Text="{Binding}"
                                                                   Foreground="{StaticResource MDNText}"
                                                                   FontWeight="Bold"
                                                                   FontSize="11"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,6,0,6"/>
                                                    </DataTemplate>
                                                </ControlTemplate.Resources>
                                                <Grid Name="PART_Root">
                                                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            Background="{TemplateBinding Background}"
                                                            CornerRadius="1">
                                                        <Border BorderThickness="2"
                                                                BorderBrush="#FFFFFFFF"
                                                                CornerRadius="1">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="*" />
                                                                </Grid.RowDefinitions>
                                                                <Button Name="PART_PreviousButton"
                                                                        Width="28"
                                                                        Height="20"
                                                                        HorizontalAlignment="Left"
                                                                        Focusable="False"
                                                                        Grid.Column="0"
                                                                        Grid.Row="0">
                                                                    <Button.Template>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Grid Cursor="Hand">
                                                                                <Rectangle Stretch="Fill" Fill="Transparent" />
                                                                                <Grid>
                                                                                    <Path Data="M288.75,232.25L288.75,240.625 283,236.625z"
                                                                                          Stretch="Fill"
                                                                                          Fill="{StaticResource MDNText}"
                                                                                          Width="6"
                                                                                          Height="10"
                                                                                          Margin="14,-6,0,0"
                                                                                          HorizontalAlignment="Left"
                                                                                          VerticalAlignment="Center" />
                                                                                </Grid>
                                                                            </Grid>
                                                                        </ControlTemplate>
                                                                    </Button.Template>
                                                                </Button>
                                                                <Button Name="PART_HeaderButton"
                                                                        FontSize="10.5"
                                                                        FontWeight="Bold"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Focusable="False"
                                                                        Grid.Column="1"
                                                                        Grid.Row="0">
                                                                    <Button.Template>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Grid Cursor="Hand">
                                                                                <ContentPresenter Content="{TemplateBinding Content}"
                                                                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                                                  Margin="1,4,1,9"
                                                                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                                                  TextElement.Foreground="{StaticResource MDNText}" />
                                                                            </Grid>
                                                                        </ControlTemplate>
                                                                    </Button.Template>
                                                                </Button>
                                                                <Button Name="PART_NextButton"
                                                                        Width="28"
                                                                        Height="20"
                                                                        HorizontalAlignment="Right"
                                                                        Focusable="False"
                                                                        Grid.Column="2"
                                                                        Grid.Row="0">
                                                                    <Button.Template>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Grid Cursor="Hand">
                                                                                <Rectangle Stretch="Fill" Fill="Transparent" />
                                                                                <Grid>
                                                                                    <Path Data="M282.875,231.875L282.875,240.375 288.625,236z"
                                                                                          Stretch="Fill"
                                                                                          Fill="{StaticResource MDNText}"
                                                                                          Width="6"
                                                                                          Height="10"
                                                                                          Margin="0,-6,14,0"
                                                                                          HorizontalAlignment="Right"
                                                                                          VerticalAlignment="Center" />
                                                                                </Grid>
                                                                            </Grid>
                                                                        </ControlTemplate>
                                                                    </Button.Template>
                                                                </Button>
                                                                <Grid Name="PART_MonthView"
                                                                      Margin="6,-1,6,6"
                                                                      HorizontalAlignment="Center"
                                                                      Visibility="Visible"
                                                                      Grid.Row="1"
                                                                      Grid.ColumnSpan="3">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                </Grid>
                                                                <Grid Name="PART_YearView"
                                                                      Margin="6,-3,7,6"
                                                                      HorizontalAlignment="Center"
                                                                      Visibility="Hidden"
                                                                      Grid.Row="1"
                                                                      Grid.ColumnSpan="3">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                </Grid>
                                                            </Grid>
                                                        </Border>
                                                    </Border>
                                                    <Rectangle Name="PART_DisabledVisual"
                                                               RadiusX="2"
                                                               RadiusY="2"
                                                               Stretch="Fill"
                                                               Fill="#A5FFFFFF"
                                                               Stroke="#A5FFFFFF"
                                                               StrokeThickness="1"
                                                               Opacity="0"
                                                               Visibility="Collapsed" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="PART_DisabledVisual" Property="Visibility" Value="Visible" />
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding Path=DisplayMode, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Calendar}}" Value="Year">
                                                        <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                                                        <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Path=DisplayMode, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Calendar}}" Value="Decade">
                                                        <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                                                        <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Setter.Value>
                        </Setter>
                        <Style.Resources>
                            <SolidColorBrush x:Key="SystemControlForegroundBaseHighBrush" Color="White" />
                            <SolidColorBrush x:Key="SystemControlForegroundBaseMediumBrush" Color="White" />
                            <SolidColorBrush x:Key="SystemControlForegroundBaseMediumHighBrush" Color="White" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.GrayTextBrushKey}" Color="White" />
                            <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="White" />
                            <SolidColorBrush x:Key="CalendarDayTitleForeground" Color="White" />
                            <SolidColorBrush x:Key="CalendarDayTitleForegroundDisabled" Color="White" />
                            <SolidColorBrush x:Key="CalendarDayTitleForegroundPointerOver" Color="White" />
                        </Style.Resources>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CalendarDayButton">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNPrimaryButtonHover}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CalendarButton">
            <Setter Property="Foreground" Value="{StaticResource MDNText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource MDNPrimaryButtonHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Application.Resources>
</Application>
